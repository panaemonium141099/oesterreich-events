-- fn-16 Liste (Option A): Karten-Details per Short-ID-Batch. v1 (JOIN-Form)
-- lief in ein 821-ms-BitmapAnd — durch 20260716085015 (LATERAL) ersetzt;
-- Datei nur für die Versions-Historie, Funktionskörper siehe v2.
SELECT 1;
